---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: chaos-mesh
  namespace: ci-cd-system
spec:
  interval: 1m
  chart:
    spec:
      chart: chaos-mesh
      sourceRef:
        kind: HelmRepository
        version: 2.6.2
        name: chaos-mesh
        namespace: flux-system
  maxHistory: 2
  install:
    crds: CreateReplace
    remediation:
      retries: 3
  upgrade:
    crds: CreateReplace
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  dependsOn:
    - name: local-path-provisioner
      namespace: kube-system
  values:
    rbac:
      create: true

    timezone: "America/Denver"

    dashboard:
      # Enable dashboard
      enabled: true

      rootUrl: "https://chaos-mesh.${SECRET_DOMAIN}"

      ingress:
        enabled: true
        certManager: true
        ingressClassName: internal
        hosts:
          - name: &host "https://chaos-mesh.${SECRET_DOMAIN}"
            tls: true
        annotations:
          hajimari.io/icon: simple-icons:grafana
        tls:
          - hosts:
              - *host

      persistentVolume:
        enabled: true
        storageClassName: local-path
        size: 5Gi
